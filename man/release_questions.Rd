% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/misc.R
\name{release_questions}
\alias{release_questions}
\title{#' Converts language to numeric
#'
#' @aliases language_to_numeric
#' @keywords internal
#' @param x A numeric or language.
#' @return A numeric value or NULL if \code{x} is language but not numeric.
language_to_numeric = function(x) {
if (is.numeric(x)) {
return(x)
} else if (is.language(x)) {
num_str = paste0(as.character(x), collapse = "")
num = suppressWarnings(as.numeric(num_str))
if (is.numeric(num)) {
return(num)
} else {
return(NULL)
}
} else {
stop("x must be either numeric or language.")
}
}}
\usage{
release_questions()
}
\description{
#' Converts language to numeric
#'
#' @aliases language_to_numeric
#' @keywords internal
#' @param x A numeric or language.
#' @return A numeric value or NULL if \code{x} is language but not numeric.
language_to_numeric = function(x) {
if (is.numeric(x)) {
return(x)
} else if (is.language(x)) {
num_str = paste0(as.character(x), collapse = "")
num = suppressWarnings(as.numeric(num_str))
if (is.numeric(num)) {
return(num)
} else {
return(NULL)
}
} else {
stop("x must be either numeric or language.")
}
}
}
