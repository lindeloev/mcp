% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp-package.R
\docType{package}
\encoding{UTF-8}
\name{mcp-package}
\alias{mcp-package}
\title{mcp: Multiple Change Point Regression in R}
\description{
Flexible and informed regression with Multiple Change Points. \code{mcp} can infer change points in models on means,
variances (and other distributional parameters), autocorrelation structure, and any combination of these, as well as the parameters of the
segments in between. All parameters are estimated with uncertainty, and prediction intervals are
supported - also near the change points. \code{mcp} supports hypothesis testing via Savage-Dickey density
ratios, posterior contrasts, and PSIS-LOO/WAIC model comparison.
}
\details{
\strong{The mcp model}

For a continuous predictor \eqn{x}, segment 1 (\eqn{x \le \Delta_1}) is a standard regression
model with the intercept defined at \eqn{x = 0}:

\deqn{\eta_{1, i} = f_1(x_i, \mathbf{\beta}_1) = \beta_{1,0} + \beta_{1,1} x_i + \dots}

For subsequent segments \eqn{k \in \{2, \dots, K\}}, each change point \eqn{\Delta_{k-1}} marks the
onset of a new segment. The model defines a segment-local predictor \eqn{X_{k,i}} measured from the
segment onset \eqn{\Delta_{k-1}} and capped at the segment end \eqn{\Delta_k}:

\deqn{X_{k,i} = \min(x_i, \Delta_k) - \Delta_{k-1}}

with \eqn{\Delta_K = \max(\mathbf{x})}. The local predictor \eqn{X_{k,i}} is zero at segment onset
(\eqn{x_i \le \Delta_{k-1}}) and plateaus at \eqn{\Delta_k - \Delta_{k-1}} for \eqn{x_i \ge \Delta_k}.

\strong{Joined (continuous) segments (\code{~ 0 + x})}

In joined models, segment \eqn{k \ge 2} has no intercept and specifies a new segment slope \eqn{\beta_{k,1}}:

\deqn{\eta_i = \eta_{1,i}(X_{1,i}) + \sum_{k=2}^K [x_i \ge \Delta_{k-1}] \cdot \beta_{k,1} X_{k,i}}

where \eqn{X_{1,i} = \min(x_i, \Delta_1)}. Because earlier segments plateau at their boundary values,
the expectation is automatically continuous across change points without requiring parameter constraints.
In both joined and disjoined segments, slope and intercept parameters are absolute values (not differences
relative to the previous segment).

\strong{Disjoined (discontinuous) segments (\code{~ 1 + x})}

When segment \eqn{k} contains an explicit intercept (\eqn{\beta_{k,0}}), it introduces a step change at
\eqn{x_i = \Delta_{k-1}} and previous segments are truncated at \eqn{[x_i < \Delta_{k-1}]}.

\strong{Extended formulas and model features}
\itemize{
\item \emph{Distributional regression:} Model residual variance and other distributional parameters across segments,
e.g., \code{~ sigma(1 + x)} or \code{~ shape(1)}.
\item \emph{Time-series residuals:} Model serial dependence using \code{ar(p)} and \code{ma(q)} terms with a generalized link-scale
recurrence that spans continuously across change points.
\item \emph{Group-level effects:} Add hierarchical (random) intercepts, slopes, and change points, e.g., \code{~ 1 + (1|id)}
or \code{1 + (1|id) ~ 0 + x}.
\item \emph{Model comparison and hypothesis testing:} Compare models using LOO-CV (\code{loo()}, \code{waic()}) and test directional
or point-null hypotheses (\code{hypothesis()}) using Savage-Dickey density ratios.
}

See \href{https://lindeloev.github.io/mcp/}{the mcp website} for worked examples and vignettes.
}
\references{
\itemize{
\item Lindeløv, J. K. (2020). mcp: An R Package for Regression With Multiple Change Points. \emph{OSF Preprints}. \href{https://doi.org/10.31219/osf.io/fzqxv}{doi:10.31219/osf.io/fzqxv}
\item Carlin, B. P., Gelfand, A. E., & Smith, A. F. (1992). Hierarchical Bayesian Analysis of Changepoint Problems. \emph{Applied Statistics}, 41(2), 389–405. \href{https://doi.org/10.2307/2347570}{doi:10.2307/2347570}
\item Stephens, D. A. (1994). Bayesian retrospective multiple-changepoint identification. \emph{Applied Statistics}, 43(1), 159–178. \href{https://doi.org/10.2307/2986119}{doi:10.2307/2986119}
}
}
\seealso{
\code{\link{mcp}}
}
\author{
Jonas Kristoffer Lindeløv \email{jonas@lindeloev.dk}
}
