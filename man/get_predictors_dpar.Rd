% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predictors.R
\encoding{UTF-8}
\name{get_predictors_dpar}
\alias{get_predictors_dpar}
\alias{get_predictor_tables}
\alias{get_predictors}
\title{Get predictors for one distributional parameter}
\usage{
get_predictors_dpar(
  data,
  form_rhs,
  segment,
  dpar,
  par_x,
  order = NULL,
  check_rank = TRUE,
  design_id = NULL
)

get_predictor_tables(model, data, family, par_x, check_rank = TRUE)

get_predictors(model, data, family, par_x, check_rank = TRUE)
}
\arguments{
\item{data}{Table-like data in long format (data.frame, tibble, data.table, etc.)
with syntactic column names.
Missing values in the response variable are imputed using the posterior predictive.
\code{\link{fitted.mcpfit}} or \code{\link{predict.mcpfit}} details how to see the imputed values.}

\item{form_rhs}{The full predictor formula of a segment, including one or
several distributional terms.}

\item{segment}{Integer. The segment number}

\item{dpar}{A distributional parameter or an \code{ar}/\code{ma} component.}

\item{par_x}{String (default: \code{NULL} which is auto-detect).}

\item{order}{Applies to \code{dpar \%in\% c("ar", "ma")}.}

\item{check_rank}{Logical scalar. Whether to stop on rank deficiency.}

\item{model}{A list of formulas - one for each segment. The general format is
\code{response ~ cp ~ predictors} (e.g., \code{y ~ 1 ~ 1 + x}), except the first segment
has no change point and uses \code{response ~ predictors}. The response and
change-point parts can be omitted (\code{cp ~ predictor} assumes the same
response; \code{~ predictor} assumes an intercept-only change point). Terms normally carry
into later segments until redefined (see details). See examples on the
\href{https://lindeloev.github.io/mcp/}{mcp website}.

\strong{1. Response (segment 1 only):}
\itemize{
\item \code{y ~ ...}: Standard continuous or count response (Gaussian, Poisson, Bernoulli).
\item \code{successes | trials(total) ~ ...}: Binomial response (\code{family = binomial()}).
\item \code{y | weights(w) ~ ...}: Observation log-likelihood weights (multiplies each observation's
log-likelihood contribution by \code{w > 0}; affects posterior inference and \code{log_lik()}, but
not predictions).
\item \code{y | trials(total) + weights(w) ~ ...}: Combine response auxiliaries using \code{+}.
}

\strong{2. Change-point modeling (\code{cp}, segments 2+):}
\itemize{
\item \code{~ 1 ~ ...} (or omitted, e.g., \code{~ x}): Population-level change point (default).
\item \code{1 + (1 | id) ~ ...}: Group-level change-point deviations around the population change point.
\href{https://lindeloev.github.io/mcp/articles/group_effects.html}{Read more}.
}

\strong{3. Regression formula (all segments):} \href{https://lindeloev.github.io/mcp/articles/formulas.html}{Read more}
\itemize{
\item \code{~ 1 + x}: Disjoined slope with a new segment intercept.
\item \code{~ 0 + x}: Joined slope (no intercept; continuous from previous segment).
\item \code{~ 1}: Plateau (intercept only, no slope).
\item \code{~ x:group + I(x^2) + exp(z)}: Extended terms, interactions, and R-side bases
(\code{scale()}, \code{poly()}, \code{splines::ns()}). Bases are evaluated before sampling and reused for \code{newdata}.
\item \code{~ 1 + (1 | id)}: Group-level intercepts (or \code{(1 + x || id)} for independent slopes and intercepts).
\href{https://lindeloev.github.io/mcp/articles/group_effects.html}{Read more}.
\item \code{~ sigma(1 + x)}: Distributional parameters on the link scale (e.g., log residual SD).
\href{https://lindeloev.github.io/mcp/articles/dpar.html}{Read more}.
\item \code{~ ar(1) + ma(1)}: Autoregressive and moving-average time-series residuals on the link scale
(accepts regression formulas, \code{series = id}, and \code{boundary}).
\href{https://lindeloev.github.io/mcp/articles/arma.html}{Read more}.
}}

\item{family}{A supported family: \code{gaussian()}, \code{binomial()}, \code{bernoulli()},
\code{poisson()}, or \code{negbinomial()}, with a supported link function; e.g.,
\code{gaussian(link = "log")}.}
}
\value{
A tibble with one row per model parameter and the columns
\itemize{
\item \code{dpar}: character.
\item \code{segment}: the segment number (positive integer).
\item \code{matrix_name}: original column name from the model matrix. Used to
diagnose collisions after parameter names are converted for JAGS.
\item \code{display_name}: user-facing parameter name used in summary functions.
\item \code{code_name}: parameter name used in JAGS and internally in mcp.
\item \code{term_key}: identifier for the formula term which generated the
coefficient. Multi-column terms share one key.
\item \code{par_type}: One of "Intercept", "dummy", or "slope". Used for setting priors and for change point indicator func.
\item \code{order}: positive integer or NA. Only relevant for \code{ar} and \code{ma}.
\item \code{explicit}: whether the distributional parameter was supplied in the formula.
\item \code{design_id}: key of the fitted component formula that produced the row.
\item \code{design_col}: column occupied by the row in that component's model matrix.
\item \code{matrix_data}: column of the design matrix less the \code{par_x} term.
}
}
\description{
This function extracts an \code{par_x}-less design matrix.
\code{par_x} will be relative to the segment onset, so it will be multiplied in in the formula
(\code{jags_code} and \code{fit$simulate()}).
}
\section{Functions}{
\itemize{
\item \code{get_predictor_tables()}: Apply \code{get_predictors_segment}
to all segments of a model.

\item \code{get_predictors()}: Return only the population predictor table.

}}
\author{
Jonas Kristoffer Lindeløv \email{jonas@lindeloev.dk}
}
\keyword{internal}
