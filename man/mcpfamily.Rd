% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/families.R
\encoding{UTF-8}
\name{mcpfamily}
\alias{mcpfamily}
\alias{is.mcpfamily}
\title{Create or Test Objects of Class "mcpfamily"}
\usage{
mcpfamily(x)

is.mcpfamily(x)
}
\arguments{
\item{x}{A family object, e.g., \code{binomial(link = "identity")}.}
}
\description{
\lifecycle{experimental}
}
\details{
Converts standard R family objects into \code{mcpfamily} objects used internally by
\code{mcp}. Supported family and link combinations include:
\itemize{
\item \code{gaussian(link = "identity")} or \code{gaussian(link = "log")}
\item \code{binomial(link = "logit")}, \code{binomial(link = "probit")}, or \code{binomial(link = "identity")}
\item \code{bernoulli(link = "logit")}, \code{bernoulli(link = "probit")}, or \code{bernoulli(link = "identity")}
\item \code{poisson(link = "log")} or \code{poisson(link = "identity")}
\item \code{negbinomial(link = "log", link_shape = "log")}
}

Note: \code{mcpfamily} objects are shipped with mcp - there is not (yet) support for user-supplied
families.
}
\section{Functions}{
\itemize{
\item \code{is.mcpfamily()}: Checks whether x is an \code{mcpfamily}.

}}
\examples{
# mcp() converts supported standard family objects automatically
data = data.frame(time = 1:6, y = exp(seq(0, 1, length.out = 6)))
fit = mcp(list(y ~ 1), data, family = mcpfamily(stats::gaussian(link = "log")), par_x = "time", sample = FALSE)
family(fit)  # Show the mcp family retained in the fit

# The converted object can also be inspected directly
mcpfamily(stats::binomial())$dpars  # Show its distributional parameters
}
\seealso{
\code{\link{family}}
}
\author{
Jonas Kristoffer Lindeløv \email{jonas@lindeloev.dk}
}
