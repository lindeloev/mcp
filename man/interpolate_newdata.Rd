% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interpolate_newdata.R
\encoding{UTF-8}
\name{interpolate_newdata}
\alias{interpolate_newdata}
\title{Returns a data.frame with all combos of predictors}
\usage{
interpolate_newdata(
  fit,
  by = NULL,
  x_values = get_x_values(fit, by),
  at = NULL
)
}
\arguments{
\item{fit}{An \code{mcpfit} object.}

\item{by}{Character vector of categorical or group-level columns to evaluate separately.
Categorical model predictors are always included.}

\item{x_values}{Numeric vector of x-values to evaluate at.}

\item{at}{Named list setting additional continuous predictors to fixed values.
They default to their observed means. For example, \code{at = list(age = 40)}.}
}
\value{
\code{tibble} with
\itemize{
\item Cols for par_x
\item unique levels combos of factorial vars
\item fixed values for additional continuous predictors
}
}
\description{
This function synthesizes predictors for all combinations of predictor values.
It is used internally in \code{plot.mcpfit()} and may be useful if you want to
build your own custom plot.
}
\details{
The \code{par_x} variable will be interpolated with higher resolution around the
change points where the values can change abruptly, but lower resolution in
between to speed up the computation.

Categorical variables and requested grouping factors are combined factorially (all level combinations).
Additional continuous predictors are held at their observed means, or at values supplied through \code{at}.
}
\examples{
\dontrun{
# Get predictors for a fit
fit = mcp_example("multiple")
newdata = interpolate_newdata(fit)

# Fit summary
fitted(fit, newdata)

# Predictions for each draw
prediction = predict(fit, newdata, summary = FALSE)
prediction[, c(".chain", ".iteration", ".draw", "x", "group", "z", "predict")]

# Custom plot
library(ggplot2)
newdata = interpolate_newdata(fit)
plotdata = fitted(fit, newdata)
ggplot(plotdata, aes(x = x, y = fitted, color = group)) +
  geom_ribbon(aes(ymin = `Q2.5`, ymax = `Q97.5`, fill = group), alpha = 0.3) +
  geom_line(lwd = 2) +
  geom_point(aes(y = y), data = fit$data)
}
}
\author{
Jonas Kristoffer Lindeløv \email{jonas@lindeloev.dk}
}
