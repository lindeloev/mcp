% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pp_check.R
\encoding{UTF-8}
\name{pp_check.mcpfit}
\alias{pp_check.mcpfit}
\alias{pp_check}
\title{Posterior Predictive Checks For Mcpfit Objects}
\usage{
\method{pp_check}{mcpfit}(
  object,
  type = "dens_overlay",
  facet_by = NULL,
  newdata = NULL,
  prior = FALSE,
  varying = TRUE,
  arma = TRUE,
  ndraws = 100,
  nsamples = lifecycle::deprecated(),
  ...
)
}
\arguments{
\item{object}{An \code{mcpfit} object.}

\item{type}{String specifying the type of plot. See \code{\link[bayesplot:PPC-overview]{bayesplot::available_ppc()}}
for available plot types, omitting the \code{"ppc_"} prefix (e.g., \code{"dens_overlay"},
\code{"ribbon"}, \code{"intervals"}).}

\item{facet_by}{Name of a grouping column used by group-level effects.}

\item{newdata}{A \code{tibble} or a \code{data.frame} containing predictors in the model.
\itemize{
\item If \code{NULL} (default), the original data is used.
\item For models with \code{ar()} or \code{ma()}: \code{fitted()}, \code{residuals()}, \code{log_lik()},
and posterior \code{predict()} condition on the response history,
so \code{newdata} must include the response. For \code{fitted()}, \code{predict()}, and
\code{residuals()}, missing response histories are supported only in the original
fitted data, using retained posterior imputations. Prior \code{predict()} and
\code{\link[rstantools:posterior_predict]{posterior_predict()}} generate fresh response
series recursively, so their \code{newdata} need only contain predictors. \code{log_lik()}
is unavailable when a missing response enters a later observed history.
\item For models with \code{y | weights()}: Require the weights column except for \code{fitted()} and \code{predict()}.
}}

\item{prior}{Logical. Evaluate prior draws (\code{TRUE}) instead of posterior draws (\code{FALSE}, default)? Useful for \code{mcp(..., sample = "both")}.}

\item{varying}{Group-level effects. One of:
\itemize{
\item \code{TRUE} All group-level deviations.
\item \code{FALSE} No group-level deviations (\code{c()}).
\item \code{"cp"} or \code{"predictor"}: All group-level deviations belonging to that part of
the model.
\item Character vector: Only include specified group-level parameters.
}}

\item{arma}{Whether to include AR and MA effects.
\itemize{
\item \code{TRUE} Compute the GARMA residual recurrence. Requires the response variable in \code{newdata}.
\item \code{FALSE} Disregard AR and MA effects. For \code{family = gaussian()}, \code{predict()} uses only \code{sigma} for residuals.
For posterior evaluation of the original data, retained JAGS imputations
supply missing GARMA histories. In models with group-level effects, this
currently requires including all such effects (\code{varying = TRUE}).
}}

\item{ndraws}{Integer or \code{NULL}. Number of posterior draws to return/summarise.
If there are group-level effects, this is the number of draws from each group.
\code{NULL} means "all". More draws trade speed for accuracy.}

\item{nsamples}{Deprecated. Use \code{ndraws} instead.}

\item{...}{Further arguments passed to \code{bayesplot::ppc_type(y, yrep, ...)}}
}
\value{
A \code{ggplot2} object for single plots. Enriched by \code{patchwork} for faceted plots.
}
\description{
Plot posterior (default) or prior (\code{prior = TRUE}) predictive checks. This is convenience wrapper
around the \verb{bayesplot::ppc_*()} methods.
}
\details{
Missing responses are omitted from the observed-data check. For
GARMA models, each replicated response series is generated recursively
rather than conditioning on the observed response history. LOO predictive
checks use posterior draws and the original fitted data, so they do not
support \code{prior = TRUE} or \code{newdata}.
}
\examples{
\donttest{
pp_check(demo_fit)
pp_check(demo_fit, type = "ecdf_overlay")
#pp_check(some_group_fit, type = "loo_intervals", facet_by = "id")
}
}
\seealso{
\code{\link{plot.mcpfit}} \code{\link{pp_eval}}

plot_pars plot_dpar pp_check
}
\author{
Jonas Kristoffer Lindeløv \email{jonas@lindeloev.dk}
}
