% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pp_check.R
\encoding{UTF-8}
\name{pp_check}
\alias{pp_check}
\alias{pp_check.mcpfit}
\title{Posterior Predictive Checks For Mcpfit Objects}
\usage{
pp_check(
  object,
  type = "dens_overlay",
  facet_by = NULL,
  newdata = NULL,
  prior = FALSE,
  varying = TRUE,
  arma = TRUE,
  ndraws = 100,
  nsamples = lifecycle::deprecated(),
  ...
)
}
\arguments{
\item{object}{An \code{mcpfit} object.}

\item{type}{One of \code{bayesplot::available_ppc("grouped", invert = TRUE) \%>\% stringr::str_remove("ppc_")}}

\item{facet_by}{Name of a column in data modeled as varying effect(s).}

\item{newdata}{A \code{tibble} or a \code{data.frame} containing predictors in the model. Weighted
Gaussian predictions and log-likelihoods also require the weights column. If \code{NULL}
(default), the original data is used.}

\item{prior}{TRUE/FALSE. Plot using prior samples? Useful for \code{mcp(..., sample = "both")}}

\item{varying}{One of:
\itemize{
\item \code{TRUE} All varying effects (\code{fit$pars$varying}).
\item \code{FALSE} No varying effects (\code{c()}).
\item Character vector: Only include specified varying parameters - see \code{fit$pars$varying}.
}}

\item{arma}{Whether to include AR and MA effects.
\itemize{
\item \code{TRUE} Compute the GARMA residual recurrence. Requires the response variable in \code{newdata}.
\item \code{FALSE} Disregard AR and MA effects. For \code{family = gaussian()}, \code{predict()} uses only \code{sigma} for residuals.
}}

\item{ndraws}{Number of posterior draws. Note that you may want to use all
draws for summary geoms, e.g., \code{pp_check(fit, type = "ribbon", ndraws = NULL)}.
LOO checks always evaluate all posterior draws to preserve their PSIS
weights; where supported, \code{ndraws} is passed to bayesplot to control the
number of plotted samples.}

\item{nsamples}{Deprecated. Use \code{ndraws} instead.}

\item{...}{Further arguments passed to \code{bayesplot::ppc_type(y, yrep, ...)}}
}
\value{
A \code{ggplot2} object for single plots. Enriched by \code{patchwork} for faceted plots.
}
\description{
Plot posterior (default) or prior (\code{prior = TRUE}) predictive checks. This is convenience wrapper
around the \verb{bayesplot::ppc_*()} methods.
}
\details{
Missing responses are omitted from the observed-data check. LOO
predictive checks use posterior draws and the original fitted data, so
they do not support \code{prior = TRUE} or \code{newdata}.
}
\examples{
\donttest{
pp_check(demo_fit)
pp_check(demo_fit, type = "ecdf_overlay")
#pp_check(some_varying_fit, type = "loo_intervals", facet_by = "id")
}
}
\seealso{
\code{\link{plot.mcpfit}} \code{\link{pp_eval}}

plot_pars plot_dpar pp_check
}
\author{
Jonas Kristoffer Lindeløv \email{jonas@lindeloev.dk}
}
