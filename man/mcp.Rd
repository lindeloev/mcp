% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcp.R
\encoding{UTF-8}
\name{mcp}
\alias{mcp}
\title{Fit Multiple Linear Segments And Their Change Points}
\usage{
mcp(
  model,
  data,
  prior = list(),
  family = gaussian(),
  par_x = NULL,
  sample = "post",
  cores = NULL,
  chains = 3,
  iter = 3000,
  warmup = 1500,
  adapt = lifecycle::deprecated(),
  inits = NULL,
  jags_code = NULL,
  seed = NULL,
  diagnostics = list(),
  quiet = FALSE
)
}
\arguments{
\item{model}{A list of formulas - one for each segment. The many examples
on the \href{https://lindeloev.github.io/mcp/}{mcp website}. But briefly:

The first formula has the format \code{response ~ predictors} while the following formulas have
the format \code{response ~ cp ~ predictors}. Here, \code{cp} names the change-point
part of the formula rather than a literal variable. The response and
change-point parts can be omitted (\code{cp ~ predictor} assumes the same
response; \code{~ predictor} assumes an intercept-only change point). Terms normally carry
into later segments until redefined (see details).

The following terms can be modeled:
\itemize{
\item \emph{Regular formulas:} e.g., \code{~ 1 + x}. \href{https://lindeloev.github.io/mcp/articles/formulas.html}{Read more}.
\item \emph{Extended formulas:} e.g., \code{~ x:group + I(x^2) + exp(z)}. \href{https://lindeloev.github.io/mcp/articles/formulas.html}{Read more}.
R-side bases such as \code{scale()}, \code{poly()}, and \code{splines::ns()} are evaluated
before sampling, and their fitted scaling or basis is reused for \code{newdata}.
\item \emph{Group-level effects (random effects):} e.g., \code{~ 1 + (1 | id)} for a group-level
intercept, or \code{~ 1 + (factor || id)} for independent intercept and
factor-contrast deviations. \href{https://lindeloev.github.io/mcp/articles/group_effects.html}{Read more}.
\item \emph{Gaussian residual standard deviation:} e.g., \code{~sigma(1)} for a simple
standard-deviation change or \code{~sigma(1 + x + group)} for more advanced
structures. Explicit \code{sigma()} formulas model log-SD, while the implicit constant \code{sigma_1} in a
model without \code{sigma()} remains on the response scale.
\href{https://lindeloev.github.io/mcp/articles/dpar.html}{Read more}
\item \emph{Time-series residuals:} link-scale observation-driven GARMA via \code{ar(p)} and \code{ma(q)},
e.g., \code{~ 1 + ar(1, series = id) + ma(1)}. Both accept an optional regression formula,
observation \code{boundary} (default 0.1), and grouping \code{series} column (see details).
\href{https://lindeloev.github.io/mcp/articles/arma.html}{Read more}.
\item \emph{Likelihood weights:} \code{y | weights(w) ~ ...} specifies
observation log-likelihood weights. Each observation's
log-likelihood contribution is multiplied by \code{w}. Weights must be positive.
Weights affect posterior
inference and \code{log_lik()}, but not the response distribution used
by \code{predict()} or prior/posterior predictive checks.
Combine with other auxiliaries using \code{+}, e.g., \code{y | trials(total) + weights(w) ~ ...}.
\item \emph{Binomial:} use \code{successes | trials(total) ~ ...} with \code{family = binomial()}.
}}

\item{data}{Table-like data in long format (data.frame, tibble, data.table, etc.)
with syntactic column names.
Missing values in the response variable are imputed using the posterior predictive.
\code{\link{fitted.mcpfit}} or \code{\link{predict.mcpfit}} details how to see the imputed values.}

\item{prior}{Named list. Names are parameter names (\code{cp_i}, \code{Intercept_i}, \code{xvar_i},
\code{sigma_1}, etc.) and the values are either
\itemize{
\item A distribution in mcp's JAGS-string syntax (e.g.,
\code{Intercept_1 = "dnorm(0, 1) T(0,)"}) indicating a
conventional prior distribution. Data-calibrated, regularizing defaults
are used where priors are not specified. These are designed for stable
estimation and prediction, but should be justified before hypothesis testing.
\code{mcp} uses conventional distribution scales rather than JAGS precision:
SD for \code{dnorm()}, scale for \code{dt()}, \code{ddexp()}, and \code{dlogis()}, and
log-SD for \code{dlnorm()}. See details.
\item A numerical value (e.g., \code{Intercept_1 = -2.1}) indicating a fixed value.
\item A model parameter name (e.g., \code{Intercept_2 = "Intercept_1"}), indicating that this parameter is shared -
typically between segments. If two group-level deviations are shared this way,
they will need to have the same grouping variable.
\item The default prior on change points is \code{dirichlet(1)} (uniform order statistics).
For a single change point, this is the Beta(1, 1) / Uniform distribution over \verb{[min(x), max(x)]}.
For multiple change points, it corresponds to a flat Dirichlet distribution over segment lengths.
You can also explicitly set \code{cp_i = "dirichlet(alpha)"} with the same positive \code{alpha} for all
change points to regularize spacing (\code{alpha > 1} penalizes change points from occurring close together,
while \code{alpha < 1} favors clustering). Under the hood, this is parameterized as an exact sequential
stick-breaking Beta chain for fast and robust sampling.
\href{https://lindeloev.github.io/mcp/articles/priors.html}{Read more}.
}}

\item{family}{A supported family: \code{gaussian()}, \code{binomial()}, \code{bernoulli()},
\code{poisson()}, or \code{negbinomial()}, with a supported link function; e.g.,
\code{gaussian(link = "log")}.}

\item{par_x}{String (default: \code{NULL} which is auto-detect).}

\item{sample}{One of
\itemize{
\item \code{"post"}: Sample the posterior.
\item \code{"prior"}: Sample only the prior. Plots, summaries, etc. will
use the prior. This is useful for prior predictive checks.
\item \code{"both"}: Sample both prior and posterior. Plots, summaries, etc.
will default to using the posterior. The prior only has effect when doing
Savage-Dickey density ratios in \code{\link{hypothesis}}.
\item \code{"none"} or \code{FALSE}: Do not sample. Returns an mcpfit
object without sample. This is useful if you only want to check
prior strings (\code{fit$prior}), the JAGS model (\code{fit$jags_code}), etc.
}}

\item{cores}{Deprecated and ignored. Configure parallel processing with a
\link[future:plan]{future} plan instead, for example
\code{future::plan(future::multisession, workers = 3)}. With the default future
plan, chains are sampled sequentially. The argument remains available for
backwards compatibility.}

\item{chains}{Positive integer. Number of chains to run.}

\item{iter}{Positive integer. Number of post-warmup draws from each chain.
The total number of draws is \code{iter * chains}.}

\item{warmup}{Positive integer. Number of initial iterations per chain which
are discarded before sampling. Set higher if needed for sampler adaptation;
use diagnostics to assess convergence.}

\item{adapt}{Deprecated; use \code{warmup} instead.}

\item{inits}{A list of initial values for the parameters. This can be useful
if a model fails to converge. Read more in \code{\link[rjags]{jags.model}}.
Defaults to \code{NULL}, i.e., no inits.}

\item{jags_code}{String. Pass JAGS code to \code{mcp} to use directly. This is useful if
you want to tweak the code in \code{fit$jags_code} and run it within the \code{mcp}
framework. R-side simulation and prediction methods continue to use the
mcp-default formulas (with warning), so they may no longer match custom JAGS code.}

\item{seed}{\code{NULL} or a positive integer. Seed for the JAGS random-number
generators. When supplied, \code{inits} must be a single named list shared by all chains.}

\item{diagnostics}{Named list of diagnostic warning thresholds. Available
elements are \code{rhat = 1.01}, \code{ess_bulk = 400}, \code{ess_tail = 400},
\code{ar = 0.10}, and \code{ma = 0.10}. An empty list uses these defaults; a partial
list overrides only the supplied values. Set an element to \code{NULL} to disable
that diagnostic, or use \code{FALSE} to disable all configurable diagnostic
warnings. In \code{\link[=summary.mcpfit]{summary.mcpfit()}}, \code{NULL} inherits the settings used to fit
the model, while a list or \code{FALSE} overrides the diagnostic footer.}

\item{quiet}{Logical. Suppress routine JAGS output and mcp sampling-status
messages? Defaults to \code{FALSE}.}
}
\value{
An \code{\link{mcpfit}} object.
}
\description{
Given a model (a list of segment formulas), \code{mcp} infers the posterior
distributions of the parameters of each segment as well as the change points
between segments. See details or \href{https://lindeloev.github.io/mcp/}{the mcp website}.
}
\details{
\strong{The mcp model}

\figure{mcp_demo.png}{options: width="500" alt="Fitted 3-segment mcp model with a plateau, joined slope, and disjoined slope"}

An \code{mcp} model divides a continuous predictor \eqn{x} into \eqn{K} segments separated by
ordered change points \eqn{\Delta_1 < \dots < \Delta_{K-1}}. In each segment \eqn{k \in \{1, \dots, K\}},
the linear predictor \eqn{\eta_i} is evaluated directly from the segment-local distance \eqn{(x_i - \Delta_{k-1})}:

\deqn{\eta_i = \begin{cases}
  \beta_{1,0} + \beta_{1,1} x_i, & \text{First segment } (k = 1) \\
  \beta_{k,0} + \beta_{k,1} (x_i - \Delta_{k-1}), & \text{Disjoined segments } (k \ge 2, \sim \texttt{1 + x}) \\
  \alpha_k + \beta_{k,1} (x_i - \Delta_{k-1}), & \text{Joined segments } (k \ge 2, \sim \texttt{0 + x})
\end{cases}}

where \eqn{\alpha_k} is the baseline level inherited continuously from the preceding segment:

\deqn{\alpha_k = \alpha_{k-1} + \beta_{k-1,1} (\Delta_{k-1} - \Delta_{k-2}) \quad (\text{with } \Delta_0 = 0)}

In all segments, slope and intercept parameters are absolute values (not changes relative to the preceding segment).

If additional continuous covariates or categorical factors are included (e.g., \code{+ z + group}),
they enter additively on their original scale (\eqn{\dots + \sum \gamma_{k,j} z_{j,i}}); only the
change-point predictor \eqn{x} is converted to segment-local coordinates.

Distributional parameters (\code{sigma()}, \code{shape()}, etc.) and autoregressive terms (\code{ar()}, \code{ma()})
follow this exact same segmented structure on their respective link scales. See more details on the \code{mcp} model in
\link{mcp-package} and on the \href{https://lindeloev.github.io/mcp/articles/formulas.html}{mcp website}.

\strong{Time-series residuals (link-scale observation-driven GARMA)}

Autoregressive (\code{ar(p)}) and moving-average (\code{ma(q)}) terms define a finite conditional recurrence
on the link scale (generalized autoregressive moving-average, GARMA). They support Gaussian (\code{identity}),
binomial (\code{logit}), Bernoulli (\code{logit}), Poisson (\code{log}), and negative-binomial (\code{log}) families.
If \eqn{b_t} is the ordinary regression predictor from the segment formulas and \eqn{\eta_t} is the
predictor including serial dependence, the recurrence decomposes into components:

\deqn{\begin{aligned}
  \text{AR}_t &= \sum_{j=1}^{p} \phi_{j,t} \left[g(y^*_{t-j}) - b_{t-j}\right] \\
  \text{MA}_t &= \sum_{k=1}^{q} \theta_{k,t} \left[g(y^*_{t-k}) - \eta_{t-k}\right] \\
  \eta_t &= b_t + \text{AR}_t + \text{MA}_t
\end{aligned}}

where \eqn{\phi_{j,t}} is the lag-\eqn{j} autoregressive (AR) coefficient at time \eqn{t},
\eqn{\theta_{k,t}} is the lag-\eqn{k} moving-average (MA) coefficient at time \eqn{t},
\eqn{g(\cdot)} is the link function, and \eqn{y^*_t} is the boundary-constrained observation:
\itemize{
\item \strong{Gaussian:} \eqn{y^*_t = y_t}.
\item \strong{Poisson / Negative Binomial:} \eqn{y^*_t = \max(y_t, b)} to prevent \eqn{\log(0)}.
\item \strong{Binomial / Bernoulli:} \eqn{y^*_t = \min(\max(y_t, b), n_t - b) / n_t}, where \eqn{b} is a boundary pseudo-count (not a proportion) and \eqn{n_t = 1} for Bernoulli.
}

Implications:
\itemize{
\item For an \eqn{N}-order component, the last \eqn{N} values \emph{before} the segment onset are input to the first \eqn{\eta_t} in the segment.
\item AR coefficients are not jointly constrained to stationarity; nor MA coefficients to invertibility.
\item See \href{https://lindeloev.github.io/mcp/articles/arma.html}{the arma vignette} for more details.
}

\strong{Notes on priors}
\itemize{
\item \emph{Ordered change point priors:} Default population-level \code{cp_i} priors are ordered and the ordering is imposed through the priors. For user-defined priors,
\code{mcp} adds truncation (e.g., \code{T(cp_1, )}) only when the prior has neither
explicit truncation nor an inherently bounded form such as \code{dunif()} or
\code{dirichlet()}.
\item \emph{Data-dependent terms:} If \code{mcp} encounters a data-dependent term like \code{min(time)}, \code{max(time)}, \code{median(response)}, or \code{mad(response)} in the prior string, they are resolved from the model data so a numerical value is passed to JAGS. The following terms are also allowed: \code{n_segments()}, and \code{n_cp()}.
The older constants \code{MINX}, \code{MAXX}, \code{MEANX}, \code{SDX}, \code{MINY}, \code{MAXY}, \code{MEANY}, \code{SDY}, and \code{N_CP} remain accepted with a deprecation warning.
\item \emph{Group-level change points:} Group-specific locations follow a hierarchical
normal distribution around their population change point, truncated so
that realized locations remain in the observed range and ordered.
\item \emph{Parameterization:} Prior strings use conventional scale parameterizations. \code{mcp} converts
these to the parameterization required by JAGS when generating code:
inverse variance for \code{dnorm()}, \code{dt()}, and \code{dlnorm()}, and inverse
scale for \code{ddexp()} and \code{dlogis()}. Use
\code{prior_summary(fit)} for resolved priors and
\code{prior_summary(fit, verbose = TRUE)} for their rules and descriptions.
}
}
\examples{
\donttest{
# Define the segments using formulas. A change point is estimated between each formula.
model = list(
  response ~ 1,  # Plateau in the first segment (Intercept_1)
  ~ 0 + time,    # Joined slope (time_2) at cp_1
  ~ 1 + time     # Disjoined slope (Intercept_3, time_3) at cp_2
)

# Fit it and sample the prior too.
# future::plan(future::multisession, workers = 3)  # Uncomment for parallel sampling
data = mcp_example_data("demo")  # Simulated data example
demo_fit = mcp(model, data = data, sample = "both", seed = 42)

# See parameter estimates
summary(demo_fit)

# Visual inspection of the results
plot(demo_fit)  # Visualization of model fit/predictions
plot_pars(demo_fit)  # Parameter distributions
pp_check(demo_fit)  # Prior/Posterior predictive checks

# Test a hypothesis
hypothesis(demo_fit, "cp_1 > 10")

# Make predictions
head(fitted(demo_fit))
head(predict(demo_fit))
head(predict(demo_fit, newdata = data.frame(time = c(55.545, 80, 132))))

# Compare to a one-intercept-only model (no change points) with default prior
model_null = list(response ~ 1)
fit_null = mcp(model_null, data = data, par_x = "time")  # fit another model here
demo_loo = loo(demo_fit)
null_loo = loo(fit_null)
loo::loo_compare(demo_loo, null_loo)

# Inspect the prior. Useful for prior predictive checks.
summary(demo_fit, prior = TRUE)
plot(demo_fit, prior = TRUE)

# Show all priors. Default priors are added where you don't provide any
prior_summary(demo_fit)

# Set priors and re-run
prior = list(
  Intercept_1 = 15,
  time_2 = "dt(0, 2, 1) T(0, )",  # t-dist slope. Truncated to positive.
  cp_2 = "dunif(cp_1, 80)",    # change point to segment 2 > cp_1 and < 80.
  Intercept_3 = "Intercept_1"           # Shared intercept between segment 1 and 3
)

fit3 = mcp(model, data = data, prior = prior)

# Show the JAGS model
demo_fit$jags_code
}
}
\author{
Jonas Kristoffer Lindeløv \email{jonas@lindeloev.dk}
}
