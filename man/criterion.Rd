% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/criterions.R
\name{criterion}
\alias{criterion}
\title{Compute information criteria for model comparison}
\usage{
criterion(fit, criterion = "loo")
}
\arguments{
\item{fit}{An mcmc.list with log-density column(s)}

\item{criterion}{One of "loo" (calls loo::loo) or "waic" (calls loo:waic).}
}
\description{
Takes a `mcpfit`` as input and computes information criteria using loo or
WAIC. Interpret directly or use `loo::loo_compare` to compare models.
}
\examples{

# Compute loos
fit1$loo = loo(fit1)
fit2$loo = loo(fit2)
fit1$loo  # view one loo


# Compare loos. Top is best. Should be several SDs better than others.
loo::loo_compare(loo1, loo2)

# Compute model weights. Higher weight is best, but read more in the
# docs.
loo::loo_model_weights(list(loo1, loo2))

}
\keyword{information,}
\keyword{loo,}
\keyword{mcmc}
\keyword{waic,}
