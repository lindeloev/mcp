% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcpfit_methods.R
\encoding{UTF-8}
\name{execute-mcp-model}
\alias{execute-mcp-model}
\alias{predict.mcpfit}
\alias{predict}
\alias{fitted.mcpfit}
\alias{fitted}
\alias{log_lik.mcpfit}
\alias{log_lik}
\alias{residuals.mcpfit}
\alias{residuals}
\title{Fitted and predicted values of \code{mcp} models fits}
\usage{
\method{predict}{mcpfit}(
  object,
  newdata = NULL,
  summary = TRUE,
  probs = TRUE,
  rate = TRUE,
  prior = FALSE,
  varying = TRUE,
  arma = TRUE,
  ndraws = NULL,
  draws_format = "tidy",
  nsamples = lifecycle::deprecated(),
  samples_format = lifecycle::deprecated(),
  ...
)

\method{fitted}{mcpfit}(
  object,
  newdata = NULL,
  summary = TRUE,
  probs = TRUE,
  rate = TRUE,
  prior = FALSE,
  dpar = "epred",
  varying = TRUE,
  arma = TRUE,
  ndraws = NULL,
  draws_format = "tidy",
  scale = "response",
  nsamples = lifecycle::deprecated(),
  samples_format = lifecycle::deprecated(),
  ...
)

\method{log_lik}{mcpfit}(
  object,
  newdata = NULL,
  summary = FALSE,
  probs = TRUE,
  rate = TRUE,
  prior = FALSE,
  varying = TRUE,
  arma = TRUE,
  ndraws = NULL,
  draws_format = "matrix",
  nsamples = lifecycle::deprecated(),
  samples_format = lifecycle::deprecated(),
  ...
)

\method{residuals}{mcpfit}(
  object,
  newdata = NULL,
  summary = TRUE,
  probs = TRUE,
  prior = FALSE,
  varying = TRUE,
  arma = TRUE,
  ndraws = NULL,
  nsamples = lifecycle::deprecated(),
  ...
)
}
\arguments{
\item{object}{An \code{mcpfit} object.}

\item{newdata}{A \code{tibble} or a \code{data.frame} containing predictors in the model. Weighted
Gaussian predictions and log-likelihoods also require the weights column. If \code{NULL}
(default), the original data is used. GARMA evaluation with \code{fitted()},
\code{predict()}, \code{residuals()}, or \code{log_lik()} conditions on the response history,
so \code{newdata} must include the response. For \code{fitted()}, \code{predict()}, and
\code{residuals()}, missing response histories are supported only in the original
fitted data, using retained posterior imputations. \code{log_lik()} is unavailable
when a missing response enters a later observed history. Use \code{\link[rstantools:posterior_predict]{posterior_predict()}}
to generate fresh response series recursively from predictor-only \code{newdata}.}

\item{summary}{Summarise at each x-value}

\item{probs}{Vector of quantiles. Only in effect when \code{summary == TRUE}.}

\item{rate}{Boolean. For binomial models, return counts (\code{rate = FALSE}) or
the observed or expected success proportion (\code{rate = TRUE}). Predictions and
count-scale fitted values require a trials column in \code{newdata}.}

\item{prior}{Logical. Evaluate prior draws (\code{TRUE}) instead of posterior draws (\code{FALSE}, default)? Useful for \code{mcp(..., sample = "both")}.}

\item{varying}{Group-level effects. One of:
\itemize{
\item \code{TRUE} All group-level deviations.
\item \code{FALSE} No group-level deviations (\code{c()}).
\item \code{"cp"} or \code{"predictor"}: All group-level deviations belonging to that part of
the model.
\item Character vector: Only include specified group-level parameters.
}}

\item{arma}{Whether to include AR and MA effects.
\itemize{
\item \code{TRUE} Compute the GARMA residual recurrence. Requires the response variable in \code{newdata}.
\item \code{FALSE} Disregard AR and MA effects. For \code{family = gaussian()}, \code{predict()} uses only \code{sigma} for residuals.
For posterior evaluation of the original data, retained JAGS imputations
supply missing GARMA histories. In models with group-level effects, this
currently requires including all such effects (\code{varying = TRUE}).
}}

\item{ndraws}{Integer or \code{NULL}. Number of posterior draws to return/summarise.
If there are group-level effects, this is the number of draws from each group.
\code{NULL} means "all". More draws trade speed for accuracy.}

\item{draws_format}{One of "tidy" or "matrix". Controls the output format when \code{summary == FALSE} (for \code{fitted()}, \code{predict()}, and \code{log_lik()}). \code{residuals()} always returns tidy output.}

\item{nsamples}{Deprecated. Use \code{ndraws} instead.}

\item{samples_format}{Deprecated. Use \code{draws_format} instead.
See more under "value"}

\item{...}{Currently ignored.}

\item{dpar}{What distributional parameter to evaluate. This is only relevant when \code{type == "fitted"}. E.g.,
\itemize{
\item \code{"epred"} (default): Expected response from the full model (or \code{NULL} for compatibility with brms etc.).
\item \code{"mu"}: The conditional mean (or success probability per trial for binomial/bernoulli models), on the link or response scale.
\item \code{"sigma"}: The standard deviation of the residuals.
\item \code{"ar1"}, \code{"ar2"}, \code{"ma1"}, \code{"ma2"}, etc. depending on which AR or MA
coefficient you want to evaluate.
}}

\item{scale}{One of
\itemize{
\item \code{"response"}: return on the observed scale, i.e., after applying the inverse link function.
\item \code{"linear"}: return on the linear-predictor (link) scale, where the linear
trends are modeled.
A linear scale is only applicable when \code{type == "fitted"} and \code{dpar} is not \code{NULL}.
}}
}
\value{
\itemize{
\item If \code{summary = TRUE}: A data frame with the draw mean and SD (\code{error}) for
each row in \code{newdata}. With posterior draws (the default), \code{error} is the
posterior predictive SD for \code{type = "predict"} and the posterior SD of the
evaluated quantity otherwise. With \code{prior = TRUE}, these are the analogous
prior summaries. If \code{newdata} is \code{NULL}, the data in \code{fit$data} is used.
\item If \code{summary = FALSE} and \code{draws_format = "tidy"}: A \code{tidybayes} \code{tibble} with all the posterior
draws (\code{Nd}) evaluated at each row in \code{newdata} (\code{Nn}), i.e., with \verb{Nd x Nn} rows. If there are
group-level effects, the returned data is expanded with the relevant levels for each row.

The return columns are:
\itemize{
\item Predictors from \code{newdata}, plus its response column when supplied.
\item Draw descriptors: ".chain", ".iteration", ".draw" (see the \code{posterior} and \code{tidybayes} packages), and \code{data_row}, the row number in the evaluated \code{newdata}.
\item Draw values: one column for each parameter in the model.
\item The estimate. Either ".epred", ".prediction", ".residual", or ".loglik" (matching tidybayes/ggdist conventions).
}
\item If \code{summary = FALSE} and \code{draws_format = "matrix"}: An \code{N_draws} X \code{nrows(newdata)} matrix with fitted/predicted
values (depending on \code{type}). This format is used by \code{brms} and it's useful as \code{yrep} in
\verb{bayesplot::ppc_*} functions.
}
}
\description{
Evaluate the model on data, either summarised (per data-row) or per draw. You
can use draws from the prior (\code{prior = TRUE}), select a distributional
parameter with \code{dpar}, and choose the response or linear-predictor scale with
\code{scale} where applicable.
}
\details{
\code{residuals(fit)} is equivalent to \code{fit$data[[mcp_columns(fit)$response]] - fitted(fit, ...)} (or \code{newdata[[mcp_columns(fit)$response]] - fitted(fit, ...)}),
but with fixed arguments for \code{fitted}: \verb{rate = FALSE, dpar = 'epred', draws_format = 'tidy'}.

\code{log_lik()} defaults to an unsummarised draws-by-observation matrix, as used
by \code{loo} and other posterior workflows.

Missing responses in the original data remain missing in the response column.
\code{fitted()} returns their expected responses, while \code{predict()} uses retained
JAGS imputations for their posterior response draws. In GARMA models these
imputations also supply the history used for later fitted and predicted rows.
}
\section{Functions}{
\itemize{
\item \code{predict(mcpfit)}: Predictive Distribution

\item \code{fitted(mcpfit)}: Expected response

\item \code{log_lik(mcpfit)}: Pointwise log-likelihood

\item \code{residuals(mcpfit)}: Residual distribution

}}
\examples{
head(fitted(demo_fit))  # Expected response for each row of demo_fit$data
head(residuals(demo_fit))  # Residuals for each row of demo_fit$data
log_lik(demo_fit)[1:3, 1:3]  # Log-likelihood at each demo_fit$data

# All of the above take a range of arguments. E.g.,:
\donttest{
head(predict(demo_fit))  # Pointwise posterior predictive
head(predict(demo_fit, probs = c(0.1, 0.5, 0.9)))  # Median and 80\% posterior predictive interval.
head(predict(demo_fit, prior = TRUE))  # Prior predictive
head(fitted(demo_fit, summary = FALSE))  # Draws instead of summary. Useful for plotting distributions.
head(fitted(demo_fit, dpar = "sigma"))  # Another model parameter

# Evaluate at novel data
novel_data = data.frame(time = c(-5, 20, 300))  # Only predictors are needed
head(predict(demo_fit, newdata = novel_data, probs = c(0.025, 0.5, 0.975)))

# Work with missing responses
missing_fit = mcp_example("missing", plot = FALSE)
fitted(missing_fit) |> dplyr::filter(is.na(y)) |> head()  # Expected responses for missing y
fitted(missing_fit, summary = FALSE) |> dplyr::filter(is.na(y)) |> head()  # Same, but all posterior draws
predict(missing_fit) |> dplyr::filter(is.na(y)) |> head()  # Posterior predictive for missing y
}
}
\seealso{
\code{\link{fitted.mcpfit}} \code{\link{predict.mcpfit}} \code{\link{residuals.mcpfit}} \code{\link{log_lik.mcpfit}}
}
\author{
Jonas Kristoffer Lindeløv \email{jonas@lindeloev.dk}
}
