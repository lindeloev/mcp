% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/comparison.R
\encoding{UTF-8}
\name{loo.mcpfit}
\alias{loo.mcpfit}
\alias{loo}
\alias{LOO}
\alias{waic.mcpfit}
\alias{waic}
\alias{WAIC}
\title{Information Criteria for Model Comparison}
\usage{
\method{loo}{mcpfit}(
  x,
  ...,
  pointwise = FALSE,
  varying = TRUE,
  arma = TRUE,
  ndraws = NULL,
  nsamples = lifecycle::deprecated()
)

\method{waic}{mcpfit}(
  x,
  ...,
  varying = TRUE,
  arma = TRUE,
  ndraws = NULL,
  nsamples = lifecycle::deprecated()
)
}
\arguments{
\item{x}{An \code{\link{mcpfit}} object.}

\item{...}{Currently ignored}

\item{pointwise}{\code{TRUE} calls calls \code{\link[loo]{loo.function}} which is slower but more memory efficient.
\code{FALSE} calls the default \code{\link[loo]{loo}}.}

\item{varying}{One of:
\itemize{
\item \code{TRUE} All varying effects (\code{fit$pars$varying}).
\item \code{FALSE} No varying effects (\code{c()}).
\item \code{"cp"} or \code{"predictor"}: All varying effects belonging to that part of
the model.
\item Character vector: Only include specified varying parameters - see
\code{fit$pars$varying}.
}}

\item{arma}{Whether to include AR and MA effects.
\itemize{
\item \code{TRUE} Compute the GARMA residual recurrence. Requires the response variable in \code{newdata}.
\item \code{FALSE} Disregard AR and MA effects. For \code{family = gaussian()}, \code{predict()} uses only \code{sigma} for residuals.
}}

\item{ndraws}{Integer or \code{NULL}. Number of posterior draws used for the
log-likelihood or information criterion. \code{NULL} uses all draws.}

\item{nsamples}{Deprecated. Use \code{ndraws} instead.}
}
\value{
a \code{loo} or \code{psis_loo} object.
}
\description{
Compare models using \code{\link[loo]{loo_compare}} and \code{\link[loo]{loo_model_weights}}.
more in \code{\link[loo]{loo}}.
}
\details{
Observationwise PSIS-LOO and WAIC are problematic for AR/MA models
because both treat individual conditional likelihood terms as validation
units. In PSIS-LOO, a held-out response also remains in the conditioning
history of later terms. Prefer leave-future-out or blocked
cross-validation, which are not currently implemented in mcp.
}
\section{Functions}{
\itemize{
\item \code{waic(mcpfit)}: Computes WAIC on mcpfit objects

}}
\examples{
\donttest{
# Define two models and sample them
# future::plan(future::multisession, workers = 3)  # Uncomment for parallel sampling
data = mcp_example_data("intercepts")  # Get some simulated data.
model1 = list(y ~ 1 + x, ~ 1)
model2 = list(y ~ 1 + x)  # Without a change point
fit1 = mcp(model1, data)
fit2 = mcp(model2, data)

# Compute LOO for each and compare (works for waic(fit) too)
fit1$loo = loo(fit1)
fit2$loo = loo(fit2)
loo::loo_compare(fit1$loo, fit2$loo)
}
}
\author{
Jonas Kristoffer Lindeløv \email{jonas@lindeloev.dk}
}
