% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/comparison.R
\encoding{UTF-8}
\name{loo.mcpfit}
\alias{loo.mcpfit}
\alias{loo}
\alias{LOO}
\alias{waic.mcpfit}
\alias{waic}
\alias{WAIC}
\title{Information Criteria for Model Comparison}
\usage{
\method{loo}{mcpfit}(
  x,
  ...,
  by_row = FALSE,
  pointwise = lifecycle::deprecated(),
  varying = TRUE,
  arma = TRUE,
  ndraws = NULL,
  nsamples = lifecycle::deprecated()
)

\method{waic}{mcpfit}(
  x,
  ...,
  varying = TRUE,
  arma = TRUE,
  ndraws = NULL,
  nsamples = lifecycle::deprecated()
)
}
\arguments{
\item{x}{An \code{\link{mcpfit}} object.}

\item{...}{Must be empty. Reserved for future use.}

\item{by_row}{\code{TRUE} calls \code{\link[loo]{loo.function}} to compute log-likelihood
contributions row-by-row (observation-by-observation), which is slower but more
memory efficient. \code{FALSE} (default) computes the full log-likelihood matrix at once.
Note that both modes calculate pointwise (observation-level) PSIS-LOO cross-validation.}

\item{pointwise}{Deprecated alias for \code{by_row}.}

\item{varying}{Group-level effects. One of:
\itemize{
\item \code{TRUE} All group-level deviations.
\item \code{FALSE} No group-level deviations (\code{c()}).
\item \code{"cp"} or \code{"predictor"}: All group-level deviations belonging to that part of
the model.
\item Character vector: Only include specified group-level parameters.
}}

\item{arma}{Whether to include AR and MA effects.
\itemize{
\item \code{TRUE} Compute the GARMA residual recurrence. Requires the response variable in \code{newdata}.
\item \code{FALSE} Disregard AR and MA effects. For \code{family = gaussian()}, \code{predict()} uses only \code{sigma} for residuals.
For posterior evaluation of the original data, retained JAGS imputations
supply missing GARMA histories. In models with group-level effects, this
currently requires including all such effects (\code{varying = TRUE}).
}}

\item{ndraws}{Integer or \code{NULL}. Number of posterior draws used for the
log-likelihood or information criterion. \code{NULL} uses all draws.}

\item{nsamples}{Deprecated. Use \code{ndraws} instead.}
}
\value{
a \code{loo} or \code{psis_loo} object.
}
\description{
Compare models using \code{\link[loo]{loo_compare}} and \code{\link[loo]{loo_model_weights}}.
more in \code{\link[loo]{loo}}.
}
\details{
Observationwise PSIS-LOO and WAIC are problematic for AR/MA models
because both treat individual conditional likelihood terms as validation
units. In PSIS-LOO, a held-out response also remains in the conditioning
history of later terms. Prefer leave-future-out or blocked
cross-validation, which are not currently implemented in mcp. When a
missing response enters a later observed GARMA history, \code{log_lik()},
\code{loo()}, and \code{waic()} are unavailable with \code{arma = TRUE}: the observed-data
likelihood requires integrating over that missing history, which mcp does
not currently implement. Setting \code{arma = FALSE} evaluates the model without
its AR/MA contribution rather than repairing the criterion for the fitted
serial model.
}
\section{Functions}{
\itemize{
\item \code{waic(mcpfit)}: Computes WAIC on mcpfit objects

}}
\examples{
\donttest{
# Define two models and sample them
# future::plan(future::multisession, workers = 3)  # Uncomment for parallel sampling
data = mcp_example_data("intercepts")  # Get some simulated data.
model1 = list(y ~ 1 + x, ~ 1)
model2 = list(y ~ 1 + x)  # Without a change point
fit1 = mcp(model1, data)
fit2 = mcp(model2, data)

# Compute LOO for each and compare (works for waic(fit) too)
loo1 = loo(fit1)
loo2 = loo(fit2)
loo::loo_compare(loo1, loo2)
}
}
\author{
Jonas Kristoffer Lindeløv \email{jonas@lindeloev.dk}
}
