#' Pre-fitted example mcp model
#'
#' `demo_fit` is an `mcpfit` object generated using `mcp_example("demo", sample = FALSE)`,
#' though with two modifications: it only has 2 x 1000 samples, and it also includes prior samples.
#'
#' It is used to quickly demonstrate `mcp` functionality in various documentation.
#'
#' @format An \code{\link{mcpfit}} object.
#' @seealso \code{\link{mcp_example}} for the model and data used to create it,
#'   and \code{\link{mcpfit-class}} for the structure and methods of fitted models.
#'
"demo_fit"
